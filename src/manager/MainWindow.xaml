<Window x:Class="WHCryptoManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="指标加密工具管理端" Height="820" Width="960"
        MinHeight="780" WindowStartupLocation="CenterScreen"
        Closing="Window_Closing">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="160"/>
            <RowDefinition Height="120"/>
            <RowDefinition Height="100"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="管理端：加密指标文件并生成客户端 exe（Win10/11 x64）"
                   FontSize="14" Margin="0,0,0,8"/>

        <Grid Grid.Row="1" Margin="0,0,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Margin="0,0,8,0">
                <TextBlock Text="软件名称（部署时的指标目录）"/>
                <TextBox Text="{Binding SoftwareName, UpdateSourceTrigger=LostFocus}" Margin="0,2,0,6"/>
                <TextBlock Text="软件版本（从 1.0 开始）"/>
                <TextBox Text="{Binding SoftwareVersion, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,6"/>
                <TextBlock Text="联系方式（过期弹窗显示管理员微信/电话）"/>
                <TextBox Text="{Binding Contact, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,6"/>
                <TextBlock Text="主密钥（64 hex，可自动生成）"/>
                <TextBox Text="{Binding MasterKeyHex, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,6"/>
                <Button Content="生成新主密钥" Click="OnGenKey" HorizontalAlignment="Left" Margin="0,0,0,6"/>
                <TextBlock Text="输出 exe 路径"/>
                <TextBox Text="{Binding OutputPath, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,6"/>
            </StackPanel>
            <StackPanel Grid.Column="1">
                <TextBlock Text="用户名（客户标识）"/>
                <TextBox Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,6"/>
                <TextBlock Text="授权天数（0=永久）"/>
                <TextBox Text="{Binding ExpireDays, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,6"/>
                <TextBlock Text="{Binding ExpirePreview}" Foreground="Gray" Margin="0,-4,0,6"/>
                <TextBlock Text="客户机器码（留空则不启用一机一码）"/>
                <TextBox Text="{Binding MachineCode, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,6"/>
                <TextBlock Text="注册码（留空则生成 exe 时自动按机器码生成）"/>
                <TextBox Text="{Binding LicenseKey, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,6"/>
                <Button Content="生成注册码" Click="OnGenLicense" HorizontalAlignment="Left"/>
            </StackPanel>
        </Grid>

        <GroupBox Grid.Row="2" Header="加密指标文件（已设密码的 .XTRD，可多选加入）" Margin="0,8,0,0" Padding="4">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <DataGrid Grid.Row="0" ItemsSource="{Binding ExtraIndicators}"
                          SelectedItem="{Binding SelectedIndicator}"
                          AutoGenerateColumns="False" CanUserAddRows="False"
                          VerticalScrollBarVisibility="Auto">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="指标文件名"
                                            Binding="{Binding FileName, UpdateSourceTrigger=PropertyChanged}"
                                            Width="180"/>
                        <DataGridTextColumn Header="文件路径"
                                            Binding="{Binding FilePath, UpdateSourceTrigger=PropertyChanged}"
                                            Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,4,0,0">
                    <Button Content="添加加密指标" Click="OnAddIndicator" Margin="0,0,8,0"/>
                    <Button Content="删除选中" Click="OnRemoveIndicator"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <GroupBox Grid.Row="3" Header="客户记录" Margin="0,8,0,0" Padding="4">
            <DataGrid ItemsSource="{Binding CustomerHistory}"
                      AutoGenerateColumns="False" CanUserAddRows="False"
                      VerticalScrollBarVisibility="Auto">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="操作时间" Binding="{Binding Time}" Width="120"/>
                    <DataGridTextColumn Header="用户名" Binding="{Binding UserName}" Width="90"/>
                    <DataGridTextColumn Header="机器码" Binding="{Binding MachineCode}" Width="180"/>
                    <DataGridTextColumn Header="注册码" Binding="{Binding LicenseKey}" Width="260"/>
                    <DataGridTextColumn Header="授权到期" Binding="{Binding ExpireDate}" Width="100"/>
                    <DataGridTextColumn Header="剩余天数" Binding="{Binding RemainingDays}" Width="70"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <GroupBox Grid.Row="4" Header="日志" Margin="0,8,0,0" Padding="4">
            <TextBox Text="{Binding LogText}" IsReadOnly="True" VerticalScrollBarVisibility="Auto"
                     FontFamily="Consolas" FontSize="11"/>
        </GroupBox>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button x:Name="genBtn" Content="生成客户端 exe" Padding="16,8" FontSize="14"
                    Click="OnGenerate"/>
        </StackPanel>
    </Grid>
</Window>
